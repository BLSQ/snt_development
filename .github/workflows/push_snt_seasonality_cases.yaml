name: Push snt_seasonality_cases

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_seasonality_cases.yaml"
      - "snt_seasonality_cases/pipeline.py"
      - "snt_seasonality_cases/requirements.txt"
      - "snt_seasonality_cases/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_seasonality_cases/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_seasonality_cases \
            --code "snt-seasonality-cases" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
