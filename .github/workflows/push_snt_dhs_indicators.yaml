name: Push snt_dhs_indicators

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_dhs_indicators.yaml"
      - "snt_dhs_indicators/pipeline.py"
      - "snt_dhs_indicators/requirements.txt"
      - "snt_dhs_indicators/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_dhs_indicators/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_dhs_indicators \
            --code "snt-dhs-indicators" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
