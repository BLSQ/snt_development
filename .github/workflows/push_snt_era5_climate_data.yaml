name: Push snt_era5_climate_data

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_era5_climate_data.yaml"
      - "snt_era5_climate_data/pipeline.py"
      - "snt_era5_climate_data/requirements.txt"
      - "snt_era5_climate_data/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_era5_climate_data/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_era5_climate_data \
            --code "snt-era5-climate-data" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
