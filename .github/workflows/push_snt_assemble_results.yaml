name: Push snt_assemble_results

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_assemble_results.yaml"
      - "snt_assemble_results/pipeline.py"
      - "snt_assemble_results/requirements.txt"
      - "snt_assemble_results/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_assemble_results/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_assemble_results \
            --code "snt-assemble-results" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
