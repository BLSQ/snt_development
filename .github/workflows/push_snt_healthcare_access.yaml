name: Push snt_healthcare_access

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_healthcare_access.yaml"
      - "snt_healthcare_access/pipeline.py"
      - "snt_healthcare_access/requirements.txt"
      - "snt_healthcare_access/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_healthcare_access/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_healthcare_access \
            --code "snt-healthcare-access" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
