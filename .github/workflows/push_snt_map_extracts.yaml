name: Push snt_map_extracts

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_map_extracts.yaml"
      - "snt_map_extracts/pipeline.py"
      - "snt_map_extracts/requirements.txt"
      - "snt_map_extracts/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_map_extracts/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_map_extracts \
            --code "snt-map-extracts" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
