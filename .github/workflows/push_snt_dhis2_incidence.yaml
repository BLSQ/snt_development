name: Push snt_dhis2_incidence

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push_snt_dhis2_incidence.yaml"
      - "snt_dhis2_incidence/pipeline.py"
      - "snt_dhis2_incidence/requirements.txt"
      - "snt_dhis2_incidence/readme.md"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: "pip"
          cache-dependency-path: "snt_dhis2_incidence/requirements.txt"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "snt-development"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push snt_dhis2_incidence \
            --code "snt-dhis2-incidence" \
            --description "${{ github.event.head_commit.message }}" \
            --link "https://github.com/BLSQ/snt_development/commit/${{ github.sha }}" \
            --yes
